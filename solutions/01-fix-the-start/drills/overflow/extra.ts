// Extra reps. Do these only if you want more of the same.
import { garden } from './garden';

// 1. Predict, as numbers:
export const predictedBirdX = 5;
export const predictedBirdY = 2;

// 2. Read with dots (wrong place again -- fix it):
export const gateX = garden.gate.x;
