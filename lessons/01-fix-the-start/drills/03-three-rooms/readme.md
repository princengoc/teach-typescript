# Three rooms

Three rooms, three doors. Every line in `answers.ts` reads from the right room
but the wrong place. Fix the dots so each name holds what the test asks for.

The last test checks that you kept the dots. Typing numbers by hand fails it.
