# Say what you see

A room is written down in `room.ts`. Before you run anything, read it and predict
the three values the tests ask about. Write each prediction in `answers.ts`, then
run `npm test`.

Do not copy expressions. Write the numbers you predict.
