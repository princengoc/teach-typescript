# Hints

**Task 1.** No `if` needed. `squares > 4` is already a boolean, so hand it
back.

**Task 2.** The same shape as task 1 with the other sign. Read yours again
before you write this one.

**Task 3.** A long shelf is never middling, so start there: when `isLong` is
true, the answer is false. `else`, the answer is whatever `isShort` is not.
