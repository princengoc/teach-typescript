# Hints

**Task 1.** No `if` needed. `squares > 4` is already a boolean, so hand it
back.

**Task 2.** The same shape as task 1 with the other sign.

**Task 3.** A long shelf is never middling, so deal with that first: when
`isLong` is true, hand back false. Inside the `else` the only shelves left are
the middling ones and the short ones.
