# 03: long or short

Shelves, measured in squares.

1. `isLong(squares)` says whether a shelf is more than 4 squares.
2. `isShort(squares)` says whether a shelf is fewer than 3 squares.
3. `isMiddling(squares)` says whether a shelf is neither. Call the two
   functions above rather than writing the numbers a second time.
