# Hints

**Task 1.** "4 or fewer" is not `< 4`. Try your answer on a load of exactly 4
before you run the test.

**Task 2.** `marketDay` is already a boolean, so it can go straight into the
brackets of an `if`. When it is true you know the answer without looking at
the load at all.

**Task 3.** The question is the one you already answered in task 2. Call
`opensOn`, then use `if` and `else` to pick which word to hand back.
