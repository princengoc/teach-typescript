# 06: the doorman

Spare drill. A door, a key, and a lock.

1. `staysOut(hasKey)` says whether someone is left outside: they are when they
   have no key.
2. `getsIn(hasKey, isLocked)` says whether they get in. An unlocked door lets
   anyone in. A locked one wants a key.
