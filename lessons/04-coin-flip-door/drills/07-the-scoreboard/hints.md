# Hints

**Task 1.** One line, no `if`. `mine > theirs` is already a boolean.

**Task 2.** The same shape with `===`.

**Task 3.** Deal with the win first and hand back `'win'`. Inside the `else`,
there are only two answers left, so a second `if` and `else` finish it.
