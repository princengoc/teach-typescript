# Hints

**Task 1.** "Type 'number' is not assignable to type 'boolean'." The function
promised a boolean and handed back a number. `isOpen` above it asks the same
question correctly. Read it.

**Task 2.** "Function lacks ending return statement." An `if` on its own only
covers one of the two answers. The card has the word for the other block.

**Task 3.** "This comparison appears to be unintentional." `'2'` in quotes is
a word, and `2` without them is a number. They are never the same value.
