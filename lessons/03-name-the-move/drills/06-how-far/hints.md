# Hints

**Task 1.** Count the gaps, not the squares. Draw three boxes in a row and
count the joins between them.

**Task 2.** The way back is the same length as the way out. Call
`stepsBetween` rather than writing the sum twice: that is what naming a move
is for.
