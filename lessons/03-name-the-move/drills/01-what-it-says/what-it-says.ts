// Read these three functions. Do not run them.

export function add(a: number, b: number): number {
  return a + b;
}

export function twice(n: number): number {
  return add(n, n);
}

export function bigger(n: number): number {
  return twice(n) + 1;
}

// Task 1: what does twice(4) hand back? Write the number here.
export const answer1 = 0;

// Task 2: what does bigger(3) hand back?
export const answer2 = 0;

// Task 3: what does add(twice(2), bigger(0)) hand back?
export const answer3 = 0;
