# Hints

**Task 1.** A square has four sides and they are all `side` long. Draw one
with `side` written on every edge if it helps.

**Task 2.** Area is rows times columns. A square garden has as many rows as
columns.

**Both.** The card line to look at is the one about `return`: the function has
to hand the number back, not just work it out.
