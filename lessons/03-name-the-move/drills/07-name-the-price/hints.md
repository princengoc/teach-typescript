# Hints

**Task 1.** One multiplication.

**Task 2.** `price` already knows the multiplication, so call it and add the
delivery to what it hands back. Writing the same sum twice means fixing it
twice when the price changes.
